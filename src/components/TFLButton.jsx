import "./TFLButton.css"

const TFLButton = ({ color }) => {
  return (<div className="line-button" style={{ backgroundColor: color }}>
    Central: Good service
  </div>);
}

export default TFLButton;